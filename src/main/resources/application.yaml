agro:
  notification:
    requests: "agro.notification.requests"
    responses: "agro.notification.responses"
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: mishavvvsweater@gmail.com
    password: nxad domu uvpb tbkc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  kafka:
    bootstrap-servers: ${BOOTSTRAP_SERVER}
    consumer:
      group-id: notificationServiceGroup
  #    consumer:
  #      enable-auto-commit: true
server:
  port: ${SERVER_PORT}


